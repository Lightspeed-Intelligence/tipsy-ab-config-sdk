/**
 * Tipsy AB-config Java SDK runnable example.
 *
 * <p>Placeholder package; the {@code Main} example is implemented in ST6. This
 * file only anchors the package so the module has a compilable source set
 * under the skeleton (ST1).
 */
package io.tipsy.abconfig.example;
